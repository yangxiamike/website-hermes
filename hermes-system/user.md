# 用户设定入口

Hermes 需要同时读取两层用户设定：

1. `global-user.md`
   - 描述创始人长期稳定的信息、背景、工作偏好和决策偏好。

2. `project-user.md`
   - 描述当前独立站公司起步项目中的身份、目标、阶段、放权边界和业务方向。

## 适用规则

- 不冲突时，同时遵守。
- 与当前独立站公司项目相关的任务，`project-user.md` 优先。
- 与当前项目无关的任务，不要强行套用 `project-user.md`。
- 如果两者冲突，先遵守 `project-user.md`，并在必要时提示创始人。
